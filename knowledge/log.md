# Knowledge Bundle Update Log

## 2026-08-03

* **Focused onboarding billing entry**: Replaced the framed Settings-style subscription panel with an unboxed onboarding introduction, one concise explanation, and direct plan or payment-recovery actions. Removed duplicated helper copy and the second "continue later" action; frontend version is now 1.3.1.

## 2026-08-02

* **Customer-satisfaction prompt**: Added a dedicated dashboard modal after completed onboarding and five sent replies, requiring a 1–5 score and written comment. The MVP reuses the authenticated feedback endpoint, Django record, Celery dispatch, Telegram, Google Sheets, and admin via the new `customer_success` kind; dismissals use a 30-day device-local cooldown, and successful submission suppresses the campaign on that device. Frontend version is now 1.3.0.
* **Onboarding refinement and contextual help**: Removed the redundant framed workspace around each setup step, changed narrow layouts to one usable scrolling task region above persistent actions, added Polish and English step-specific help, clarified rules versus knowledge, and made the Messages location of reply suggestions explicit. Follow-up refinement moved help into a quiet title action; replaced settings-style cards with direct onboarding controls while leaving their full Settings variants intact; lists real imported offers instead of abstract product-knowledge metrics; makes the two required previews an explicit checklist; enables multi-file shop-knowledge batches; and marks the optional rules step reviewed after continuing. Also tightened onboarding semantics, accessible field names, touch targets, required product copy, and adjacent web runtime warnings.
* **Customer-success feedback research**: Traced the authenticated feedback-report pipeline from its Help Center and global modal entry points through Django persistence, Celery, Telegram, Google Sheets, and admin. Proposed a separate structured, lifecycle-aware satisfaction survey that reuses delivery infrastructure while adding server-owned eligibility, cooldowns, idempotency, rating data, and follow-up routing; details in `plans/customer-success-feedback-pipeline-research.md`.
* **Landing-page analytics evaluation**: Compared Cloudflare Web Analytics, Umami, PostHog, and Plausible against the landing app's free-tier, performance, attribution, privacy, and operational constraints. Recommended Cloudflare for basic visit/RUM data, Umami Cloud Hobby when UTM and conversion tracking are required, and PostHog only when broader product analytics is imminent; details in `references/landing-page-analytics-evaluation.md`.
* **Guided onboarding workspace**: Replaced the fullscreen setup modal with a resumable six-step app workspace, conditional billing recovery, background product-knowledge progress, focused shop-knowledge and rules inputs, suggestions-first assistant setup, and two real simulator outcomes required before activation.

## 2026-07-28

* **Backend review faza 14 (prod hardening)**: Removed public nginx `/media/` serving (KB documents and audit exports were unauthenticated) in favor of a staff-only download endpoint, added a CI coverage gate and a production-image `check --deploy` job, fixed migration linting on main pushes, and added Django admin for billing, onboarding, postbuy, and feedback.
* **Backend review faza 13 (część B/C/D)**: Postbuy, referral, simulator, waitlist, and feedback moved to the global `{code, message}` error contract with Polish messages (whole backend now consistent), postbuy pagination validates input, and template validation messages are Polish; contract details in `frontend/api-contract-changelog.md`.
* **Backend review faza 13 (część A)**: Postbuy order scans now survive poisoned events and sweep stuck PENDING messages, simulator chat gained per-user cost throttles (10/minute, 100/day), and monthly referral rewards use accumulating Stripe balance credits instead of discount replacement; a deferred task tracks flagging high simulator usage without a subscription.
* **Backend review faza 12 (notifications)**: Localized push notifications from user language preference with explicit "messages left unanswered" usage-limit copy and `/messages` deep link, added backoff retries for transient delivery failures, and fixed Expo receipt reconciliation so resolved receipts leave the scan window; details in `plans/backend-review-history.md` and `frontend/api-contract-changelog.md`.

## 2026-07-27

* **Simulator/production bot parity**: Established the autoresponder single-turn
  decision engine as the canonical behavior for Allegro production replies, the
  Rules-page simulator, and bot evals. Documented identical history, DRE/RAG,
  product/global knowledge, buyer-facing formatting, escalation semantics, and
  the exact three-attempt AI retry policy.
* **Side-effect and telemetry boundary**: Documented that simulator turns bypass
  subscriptions and never create production attempts, cursors, suggestions,
  escalations, notifications, or customer usage, while retaining technical
  token/cost telemetry under `execution_mode=simulator`.
* **Simulator and audit API contracts**: Added the saved favorite `offer_id`,
  expanded simulator trace evidence, the `generation_failed` escalation
  category, and execution-mode filtering, grouping, and export compatibility.
## 2026-07-25

* **State-dependent registration header**: Extracted the shared auth title and description presentation into `AuthHeader` so registration can replace that introduction with its email-verification state without duplicating login styling.

* **Automatic SemVer rule**: Required agents to bump the app version for every included application change, select the smallest appropriate SemVer increment, synchronize shipped version sources, and report the selection and rationale at handoff.

* **English-only knowledge and writing policy**: Translated remaining Polish knowledge passages into English and made English mandatory for repository documentation, code, identifiers, comments, docstrings, tests, and other engineering text; explicitly localized customer-facing content remains allowed where the product requires it.

* **Mailjet sender regression fix**: Restored required `MAILJET_FROM_EMAIL`
  forwarding and explicit sender assignment for every transactional email
  after Mailjet rejected Django's `webmaster@localhost` fallback before
  accepting messages.
* **Transactional-email production hardening**: Enforced Mailjet credentials and the Anymail Mailjet backend in production, required an absolute HTTPS notification origin, escaped account-deletion request text before administrator-template rendering, removed discount codes from Mailjet metadata, migrated legacy Resend delivery labels to Mailjet, and aligned landing Compose waitlist forwarding.
* **Localized waitlist and notification templates**: Replaced raw waitlist and system-notification email bodies with six required Mailjet templates selected by persisted `pl`/`en` language. Waitlist templates receive only discount data; escalation and usage-limit templates receive only their declared action variables.
* **Localized account-deletion email templates**: Replaced the shared conditional Mailjet template with five purpose-specific templates: Polish and English owner verification, one Polish administrator request, and Polish and English completion. Deletion requests now persist `pl`/`en`; each template receives only its declared variables.
* **Localized account-confirmation email templates**: Replaced the original single Mailjet template setting with required Polish and English template IDs selected from the app locale carried through registration and resend requests; confirmation templates receive only `confirmation_url`.

## 2026-07-24

* **Typeface correction**: Corrected the frontend design system to document the shipped Inter font family, its 400/500/600/700 root loading, and the actual Tailwind family mappings. The previous Roboto guidance was stale.
* **Portable visual reference**: Rewrote the frontend design-system concept around the recognizable Superseller look, exact reusable values, brand mark, component character, and medium-specific adaptation. Removed stale route behavior and contradictory app-screen recipes so agents can reproduce the visual language in new interfaces, landing pages, emails, and branded assets.

## 2026-07-22

* **Backend review faza 11 (B/C/D)**: Billing and onboarding errors moved to the global `{code, message}` contract with Polish messages (`billing.*`/`onboarding.*` codes, Stripe webhook exempt), onboarding state polling slimmed to a single aggregate pass, KB document versioning unified behind a locked `rag.services.next_document_version`, and plan-price caching skips incomplete Stripe responses; contract details in `frontend/api-contract-changelog.md`.
* **Backend review faza 11 (billing/onboarding, bugi A)**: Stripe webhook ignores out-of-order events via `last_event_created`, entitlement mutations run under row locks, and product KB generation moved to a Celery task with a 202 + polling contract (`product_kb_in_progress`, `product_kb_started_at`); details in `plans/backend-review-history.md` and `frontend/api-contract-changelog.md`.
* **Proxy-independent backend healthcheck**: Made the production web probe use a direct loopback connection so inherited `HTTP_PROXY` settings cannot route an internal readiness check away from Gunicorn and incorrectly mark a healthy container as unhealthy.

## 2026-07-21

* **Local webhook tunnel and delivery verification**: Documented temporary and stable Cloudflare Tunnel setup for local Django webhooks, including host and container reload requirements, plus success, controlled-failure, Telegram-deduplication, and Mailjet-reconciliation tests for account-deletion email delivery.
* **Account-deletion email observability**: Split Mailjet API acceptance from provider delivery with PII-free PostgreSQL state, authenticated Anymail webhooks, stale-message reconciliation, Django-admin visibility, and idempotent Telegram alerts; completion-email failure no longer misreports a completed deletion as failed.
* **Container environment propagation**: Required backend agents to forward new or renamed environment variables through every consuming local and production Compose service, including shared Django/Celery anchors and adjacent proxy services, with regression coverage tied to `PRODUCTION_REQUIRED_ENV`.

## 2026-07-16

* **Final product name**: Confirmed Superseller as the final customer-facing product name, removed placeholder and working-name language from the product overview and repository README, and narrowed the remaining marketing confirmation boundary to the public domain rather than the brand name.
* **Shared TanStack test fixture**: Centralized query-hook test clients and providers in `frontend/testUtils/queryClient.ts`, with deterministic no-retry/no-background-refetch defaults and zero query/mutation cache retention so Jest workers exit cleanly; `jest.afterEnv.ts` now clears registered clients globally after each test, while `jest.setup.ts` remains limited to early environment-wide mocks.
* **Rule-management feature split**: Split `features/rule-management` into `features/rules`, `features/knowledge-base`, and `features/simulator` so each nav surface owns one feature per the one-feature-per-surface shape; the `/rule-management` route path is preserved and now renders the rules feature. Onboarding's cross-feature imports were retargeted to the new paths (promoting them to `shared/` remains open).
* **Shared HelpPanel**: Promoted the duplicated per-view help/FAQ block (title + intro + optional feature content + single-open FAQ accordion) into `shared/components/HelpPanel`; the Rules, Knowledge Base, Simulator, and Post-buy help panels are now thin wrappers passing their own i18n copy, and the Rules panel adopts the same compact density as the others.
* **Product-knowledge sync on the data layer**: Replaced the hand-rolled fetching in `shared/hooks/useProductKnowledgeSync` with `api/queries/onboarding.ts` hooks — both onboarding hooks cache the full state response under `qk.onboardingState()` and narrow via `select`, the sync summary is the named `ProductKnowledgeSyncSummary` type gated on Allegro connection, and the sync action is a mutation that invalidates the onboarding state. The unused `reloadMeta`/`hasSyncedWithoutActiveOffers` surface was dropped.

* **Shared inline field tooltip**: Promoted the settings info trigger into `shared/components` and moved the escalation-message helper copy behind that accessible tooltip beside its label.
* **Lazy simulator transport domain**: Split the AI SDK streaming transport into the dedicated `frontend/api/simulator-chat` domain. The general simulator barrel remains safe for eagerly loaded favorites queries, while `SimulatorSection` is the only runtime consumer of the chat domain so `ai` and `@ai-sdk/react` stay in the route's lazy web chunk.
* **Shared auth layout**: Aligned login and registration with the product workspace by adding a shared auth frame with the enlarged brand centered above one focused bordered form panel, language switching anchored at the bottom-right of the view, a quiet app canvas, compact title hierarchy, large accessible controls, wrapping account-switch links, and tinted inline error feedback.
* **Message Center workspace**: Aligned Messages with the shared fixed split layout: equal attention and inbox cards own the one-third column, the full conversation owns two-thirds, and each region scrolls independently. Thread placement is snapshotted for the mounted view so clearing an unread badge does not move the row, while chat bubbles now render complete message bodies without line clamping.
* **Rules automation ownership**: Moved automatic-reply mode and escalation-message controls from general Settings into one compact Settings card at the top of the Rules auxiliary column; scan frequency remains in general Settings, while the Rules FAQ keeps the remaining independently scrolling space.
* **After-sales service workspace**: Removed delivery history and its request from the Post-buy route, moved the master switch into a compact auxiliary Settings card, and aligned the remaining message and priority-rule configuration with the fixed two-thirds/one-third workspace and shared responsive help pattern. Replaced the three-item priority-message preview and browse modal with the complete virtualized list, using Knowledge Base-style divider rows, quiet metadata, and inline edit/delete actions.
* **Fixed split-workspace guidance**: Added the shared Rules, Knowledge Base, and Simulator layout contract to the frontend design system: consistent `max-w-7xl` two-thirds/one-third composition, fixed desktop height with explicit internal scroll ownership, non-squishing accordions, stacked narrow layouts, unboxed FAQ treatment, and concise feature-specific hierarchy.

## 2026-07-15

* **Safe knowledge interview commands**: Added disclosure-versioned, append-only voice consent; tenant-isolated and idempotent knowledge-card revision/confirm/reject commands with optimistic concurrency; a consent-bound unified-WebRTC call lifecycle with server-side hangup; and an exportable typed React workspace for readiness, coverage, and card validation. Live WebRTC remains gated on the model tool-call bridge.
* **Knowledge builder foundation**: Started implementation of the OpenAI-only knowledge interview with a new Django domain for sessions, cards, revisions, evidence, P0 coverage, readiness, and snapshots; added authenticated start/resume, current-session, private card-list, readiness and coverage APIs plus tested fail-closed OpenAI Realtime configuration and tool contracts.
* **OpenAI knowledge builder plan**: Locked the OpenAI-only, web-first design that replaces mandatory knowledge uploads with a resumable voice interview, validated and versioned knowledge cards, P0 readiness coverage, central Allegro policy dependencies, and atomic compilation into RAG, guardrails, routing, and live tools; implementation begins with the backend domain and Realtime contracts.
* **Verified account deletion**: Implemented the public landing request and explicit confirmation pages, shared Expo web/iOS/Android Settings entry, non-enumerating Django request API, one-time email ownership verification through a configurable Mailjet template, superuser fulfillment, immediate Stripe cancellation, account/file cleanup, completion notification, privacy copy, and deployment configuration.
* **Simulator workspace**: Redesigned Simulator as the same fixed-height conversation workspace used by Messages: a two-thirds transcript/composer pane plus a one-third auxiliary column. The route participates in the app shell's fixed-content set, so the page itself never scrolls on desktop; the transcript and auxiliary column own independent scroll regions. The auxiliary column contains product selection and compact save/open/restart actions, an always-present collapsed Decision Details accordion with a pre-response empty state, and the unboxed FAQ; narrow layouts stack the same surfaces and expose the shared help jump. Simulator bubbles and composer now match the Message Center vocabulary; the decision inspector uses concise key/value rows with applied rules and confidence diagnostics collapsed behind their counts, never resizes the conversation, and requires no post-response toolbar button. Product changes immediately start a fresh test conversation, and saved conversations continue to use the existing list modal.
* **Knowledge Base workspace**: Redesigned the route as a fixed-height two-column workspace with the filterable, internally scrolling document list on the left and an aggregated coverage map plus unboxed help/FAQ on the right; narrow layouts expose a help jump and stack the auxiliary content below the list. Desktop graph/help and manual/learned-rule splits use fixed equal flex allocations, with help content scrolling internally so accordion expansion cannot resize sibling panes; the shared end-aware scroll cue reinforces native indicators across help/FAQ, both rule cards, and the virtualized Knowledge Base document list. Document rows use divider-only structure with update time and scope in one quiet metadata line; only non-ready statuses remain visibly called out, actions stay inline, and fragment count plus version live in a preview header that mirrors the row hierarchy. The map uses SVG-safe organic blobs, curved connections, app-token primary/info/neutral fills, full external left/right offer labels with safe bounds, and a deterministic force layout instead of a uniform radial ring; clicking a blob filters the list and reveals an accessible clear-filter action. Moved product-knowledge generation out of Settings into a compact action card above the map, collapsed the last-run breakdown into an accessible tooltip behind the visible generation timestamp that stays open through a web click while hovered, removed the offer-limit selector, and made Generate open an explanation-only confirmation modal before the shared sync flow processes all active offers; onboarding retains its existing presentation.
* **Compact accessible app density**: Standardized the product on a 15px secondary/control tier and 16px reading tier with explicit line heights, reduced oversized heading jumps, changed desktop page rhythm from 32px to 24px, and normalized standard card padding to 16px. Main forms and primary controls retain their 40px default height. Navigation destinations moved up to 15px with medium icons after the 14px/small-icon version proved too small, while 12px group labels preserve hierarchy.
* **Collapsible desktop navigation**: Added an accessible arrow control that switches the 240px desktop sidebar to a 72px icon rail. The collapsed state retains active-route styling, group separators, utility actions, the account avatar, and an unread dot while hiding labels and email; the mobile drawer remains unchanged.
* **Compact navigation scale**: Reduced sidebar and drawer destination labels from 16px to 14px, moved navigation icons from medium to small, tightened desktop row and category spacing, and retained 40px minimum drawer targets for touch accessibility.
* **Workflow-grouped navigation**: Reorganized the desktop sidebar and compact drawer into overview, automation, customer-service, and admin sections; split the combined Rules surface into separate Rules, Knowledge Base, and Simulator routes while preserving `/rule-management` for existing links. Rules now contains manual and learned rules together, and Messages sits beside After-sales service in the customer-service group. Added Polish and English labels and corrected onboarding copy that still described the simulator as a Rules tab.
* **Saved simulator conversations exposed**: Built the UI on top of the spike-023 data layer, moving saved conversations from **Backend-ready** to **Current** in the product feature matrix and capability list. Save and open actions live in the simulator's `SectionHeader`; the list is a `ListModal`, and opening one replaces the live chat by seeding a fresh `chatId` — the existing reset mechanism — so the transcript renders through the existing chat/trace components and never a second renderer. Resolved the spike's open questions: titles are left to the service's auto-generation (no rename endpoint exists, so a bad title is currently permanent), and opening over a non-empty chat confirms first. No favorites cap is enforced on either side. The spike's `useSimulatorFavoriteQuery` was replaced by a `useLoadSimulatorFavorite` mutation: opening is a one-shot press action, so a gated `useQuery` would have needed an id in state plus an effect to catch its result.
* **Trivial-test ban**: Added a "Tests Worth Writing" coding rule against tests that only restate the implementation — field-by-field mapper assertions, pass-through wrappers, and schema shapes the type system and boundary parse already enforce — and dropped `frontend/api/simulator/mappers.test.ts` (the spike-023 test that prompted it) and `frontend/api/queries/keys.test.ts` (which re-asserted each `qk` tuple literally and, despite its name, never exercised TanStack's prefix matching). Endpoints, mappers, and query hooks need no test by default; branching, parsing, cache behavior, and pinned regressions still do. `parse.test.ts` and `formatters.test.ts` were reviewed against the rule and kept: the former is the only guard on the documented no-leak invariant in `parseBoundary`, the latter covers locale formatting the rule names as worth testing.
* **Shared-utility check**: Added a cross-cutting coding rule to inspect `@/shared/lib` and `@/shared/hooks` before introducing a utility or helper, reusing an existing fit rather than duplicating it locally.
* **Vercel deploy target removed**: Deleted `frontend/vercel.json`, an Expo-scaffold leftover no deploy path ever used — the web export ships only through `frontend/Dockerfile` into an nginx image, with no CI step or deploy script referencing Vercel. The security headers mirrored into it earlier the same day went with it, and the web-hardening section of `security.md` returns to naming `nginx.conf` as the single home of the CSP, removing a two-file sync obligation that guarded nothing. Plan 006 is REJECTED per its own STOP condition.
* **Roboto product font**: Adopted Roboto as the product typeface — loaded at the app root via `expo-font`/`@expo-google-fonts/roboto` and mapped `font-heading`/`font-body` to `["Roboto"]` in `tailwind.config.js` — and updated the design-system typeface section (4.1) to document the load point, the shared-mapping change-in-one-place rule, and the per-platform weight-fidelity caveat.
* **Lint-enforcement claim correction**: Corrected `react-components.md`, which credited a non-existent `no-restricted-syntax` ban on `FunctionDeclaration`; `func-style: ["error", "expression"]` already errors on the `function` keyword, so the doc now cites only the mechanism that actually ships in `eslint.config.js`.

## 2026-07-14

* **Loose documentation consolidation**: Verified the workspace server-debugging note against its existing operations runbook, removed the duplicate, repaired the runbook's canonical metadata and citations, and tightened diagnostic-log permissions and retention; confirmed the main app product register's guidance was already represented in the product overview and frontend design system, then removed the superseded register.
* **Coolify API routing**: Removed the production backend's extra Compose `default` network, retained the external `coolify` network required by PostgreSQL, and pinned Traefik's nginx upstream to that reachable network to prevent intermittent post-deployment 504s.
* **Allegro OAuth permissions**: Documented the production and sandbox Developer App scopes required for account identity, offers, orders, and messaging, including the need for fresh seller authorization after scope changes.
* **Waitlist checkout fallback**: Made subscription checkout retry without a stale automatic waitlist promotion code, invalidate the missing provider ID, and remove waitlist metadata so the undiscounted purchase is not falsely recorded as a redemption; unrelated Stripe errors remain blocking.

## 2026-07-13

* **Development scan shortcuts**: Added production-guarded `make scan`, `make scan-sync`, and `make scan-logs` workflows backed by a tested Django management command, with explicit email selection or safe inference when exactly one auto-reply account is enabled.
* **Stripe webhook startup enforcement**: Made a valid-looking webhook signing secret mandatory whenever Stripe billing is enabled, and made production API startup verify the configured Dashboard endpoint's account mode, enabled status, exact URL, and required event coverage through Stripe before serving traffic.
* **Langfuse hardening and self-hosted storage**: Documented mandatory R2-backed event storage for self-hosted Langfuse, its web/worker configuration and incident checks; reconciled trace privacy with recursive fail-closed PII scrubbing and hashed external identifiers, sampling validation, true root observations, generated trace-ID correlation, and backward-compatible Redis audit payload persistence.

## 2026-07-12

* **Subscription upgrade and cancellation state**: Added the Settings action
  that takes eligible Basic subscribers to Stripe's hosted Pro-upgrade
  confirmation while preserving billing cadence, made scheduled cancellation
  messaging include the final active date, and documented the required Stripe
  Customer Portal plan-switch catalog and verification steps.

## 2026-07-11

- **Frontend naming lint**: Added ESLint enforcement that app-code variable identifiers stay camelCase/PascalCase/UPPER_CASE while `frontend/api/**` remains the allowed boundary for snake_case wire-shape identifiers.
- **Notification frontend architecture**: Moved notification endpoints into the one-directory-per-endpoint API shape, added Zod response validation and snake_case-to-camelCase boundary mapping, and routed Settings reads and mutations through TanStack Query with centralized keys and cache invalidation.
- **Langfuse traceability projection**: Added optional Langfuse v4 traces for the autoresponder run hierarchy, shared OpenRouter generations and embeddings, pseudonymous user/session correlation, local trace-ID persistence, production metadata-only enforcement with export masking, and failure isolation that keeps Langfuse outside the customer-reply success path; local `ai_audit` remains authoritative.
- **Non-blocking AI audit persistence**: Replaced synchronous reply-path audit writes with an allowlisted in-memory run collector, one best-effort Redis/Celery batch per run, idempotent transactional persistence, and eventual `AutoReplyAttempt` linking; the existing Celery worker consumes the default and dedicated `ai_audit` queues, and queue or audit-storage failures no longer affect reply delivery.
- **Legacy knowledge migration**: Migrated the backend review history, frontend API contract changelog, and Stripe activation runbook into indexed OKF concepts; merged the product register's positioning, design, and accessibility guidance into canonical product/frontend concepts; reconciled live API and Stripe details; and removed the four superseded root/backend files.

## 2026-07-10

- **Referral modal**: Replaced the dedicated referral route with a compact customer modal opened from a highlighted utility-navigation action; documented that the UI exposes the registration link, code, and aggregate statuses without promising a fixed reward value.
- **Frontend shared-first rule**: Strengthened frontend guidance to require checking `@/shared/components`, panels, UI primitives, hooks, and libs before building UI; added `SegmentedControl` to the shared UI catalogue for billing cadence/filter-mode style pickers.
- **Notification email logo delivery**: Documented that the built-in notification email embeds the app logo as a `cid:` inline attachment instead of a `data:` URI so common email clients render it.
- **Notification email brand layout**: Documented that the built-in notification email uses the app favicon mark, system-sans Superseller wordmark, compact dark card spacing, and orange primary CTA.
- **Notification email fallback template**: Documented the repo-owned text/HTML template used when `MAILJET_NOTIFICATION_TEMPLATE_ID` is blank, including its readable action-card layout for escalation and usage-limit notifications.
- **Mailjet notification templates**: Documented the optional `MAILJET_NOTIFICATION_TEMPLATE_ID`, its merge variables, and the rule that stored payload reason codes become human-readable labels in email bodies and template data.
- **Mailjet notification delivery**: Documented that Mailjet notification emails use one provider tag, the notification event type, because Mailjet rejects multiple tags; generic notification context stays in metadata.
- **Notification settings and device registration**: Documented user-facing notification preferences, provider capabilities, safe push-endpoint listing, and browser/native push registration in Settings.
- **Allegro OAuth error modal timing**: Documented that account-assignment OAuth errors are queued until the popup closes and are shown only in the shared modal on settings/onboarding, with no red inline alert behind it.
- **Allegro web OAuth callback routing**: Documented that web-started OAuth flows store `{FRONTEND_BASE_URL}/allegro-auth` in signed state so callback errors return to the active settings/onboarding screen, and that the active hook consumes stored popup errors to trigger the existing account-assignment modal even if browser events are missed.

## 2026-07-09

- **Agent guidance consolidation**: Moved frontend convention docs from `frontend/docs/conventions/` into OKF concepts under `knowledge/agent-guidance/frontend/`, moved the historical frontend redesign plan into `knowledge/plans/`, moved backend test/config/error-contract guidance into `knowledge/backend/agent-rules.md`, compacted `AGENTS.md` into a knowledge table of contents, symlinked `CLAUDE.md` to `AGENTS.md`, corrected README validation commands, and exposed `frontend/.claude/skills/` through `.agents/skills` and `frontend/.agents/skills` compatibility symlinks.
- **Frontend CI trigger cleanup**: Limited `Frontend CI` push runs to `dev` and `main` while keeping pull request checks for those target branches, avoiding duplicate feature-branch push and PR checks for the same commit.
- **Dependency updates**: Added `.github/dependabot.yml` so Dependabot monitors the app's npm, pip, Docker, and GitHub Actions dependency surfaces and opens update pull requests against `dev`.
- **Frontend redesign Phase 6 (finalize)**: Reconciled `agent-guidance/frontend-rules.md` with the shipped frontend — replaced the stale Supabase/old-architecture body with an index into the modular `frontend/docs/conventions/` rule set, corrected auth to Django JWT, listed the real `api/` domains, and reconciled the testing claim (no suite yet; CI gates are the floor). Repointed its `resource`/`source_paths` and refreshed review dates.
- **Frontend conventions completed**: Added `frontend/docs/conventions/testing.md` (deferred jest-expo + RTL harness, aspirational) so the cross-linked set is complete; added the `add-feature` authoring skill under `frontend/.claude/skills/`.
- **Frontend setup docs**: Added a real `frontend/README.md` (setup/run/verify/layout) and documented `EXPO_PUBLIC_ALLEGRO_SANDBOX` in `frontend/env.example`. App metadata (`app.config.ts`) and the archived auth-migration plan were already reconciled in prior work.
- **Allegro account-claim backfill and repair**: Documented that occupied Allegro account lookup uses the indexed persistent assignment table, while legacy OAuth tokens are hydrated through backfill or owner status checks instead of runtime global scans during new connections.
- **Allegro OAuth error modal propagation**: Documented that web OAuth callback errors are forwarded to the active settings/onboarding screen so the account-assigned-to-another-user modal and report-problem draft appear after failed connection attempts.
- **Allegro OAuth session isolation**: Documented that Allegro status and disconnect endpoints must ignore browser-session tokens owned by another app user, preventing cross-user integration metadata display and accidental token removal in shared-browser scenarios.
- **Persistent Allegro account assignment**: Documented that Allegro account IDs are claimed persistently per app user, remain assigned after OAuth token disconnect, block other users until support release, and expose a feedback-report path for disputed assignments.
- **Backend deployment health**: Documented the current Coolify internal PostgreSQL host for backend `DATABASE_URL`, clarified that production app containers must use the internal `coolify` network address instead of localhost/SSH tunnel endpoints, and recorded the nginx Coolify network requirement in deployment configuration.
- **Backend database diagnostics**: Fixed the production database readiness helper to add the Django source directory to `sys.path` before `django.setup()`, so container startup logs report real database errors instead of failing to import `allegrobot`.

## 2026-07-08

- **Backend env docs**: Documented native-auth JWT/cookie variables and clarified notification environment variables in the backend overview, system notifications concept, and production deployment table.
- **Recovery**: Restored the OKF v0.1 knowledge bundle after accidental deletion in a separate agent conversation.
- **Cleanup preserved**: Kept coding guidance English-only and did not copy the old Polish Claude compatibility notes.
- **Legacy cleanup**: Removed migrated legacy documentation files after confirming their content exists in OKF concepts. Kept only active entry points: `AGENTS.md`, `CLAUDE.md`, `README.md`, and `frontend/.cursor/rules/rules.md`.
- **Merge cleanup**: Resolved the dev/OKF merge by keeping the OKF README entry point, preserving deleted legacy docs, and clarifying that the native auth migration plan now lives in OKF.
- **Superseller rename**: Removed the remaining legacy product names and repository-name references across app metadata, mobile identifiers, backend defaults, eval labels, agent guidance, and OKF concepts.
- **Brand icons**: Added generated web, iOS, and Android icon assets based on the canonical storefront logo and documented their Expo configuration in the frontend design-system concept.
