# Campaign artifacts

The LinkedIn Campaign Studio writes each review-required campaign to a dated subdirectory here. Generated subdirectories are ignored by Git by default; add one deliberately only when it is useful as a reviewed example.

Nothing in this directory is approved for publication merely because it exists.
